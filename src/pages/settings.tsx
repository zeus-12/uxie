const UserSettings = () => {
  return (
    <div>
      user settings page, letting users customise: 1. behaviour on adding
      highlight : whether it should get added to the md editor as well 2.
    </div>
  );
};
export default UserSettings;
